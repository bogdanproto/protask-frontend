import styled from 'styled-components';

export const BtnContainer = styled.div`
    margin-left: auto;
    display: flex;
    align-items: center;
    gap: 8px;

    button {
        line-height: 0;
    }
`;